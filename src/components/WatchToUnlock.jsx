/**
 * WatchToUnlock — no-op stub.
 * Ad-based credit system has been removed. This file is kept to avoid breaking imports.
 */
export default function WatchToUnlock({ isOpen, onClose }) {
  return null;
}